extend layout 

block content
  nav.contributors
    ul
      each contributor in contri
        li
          a(href=contributor.url) #{contributor.name}
  hr
  a(href='/contributor/create') Create New User
